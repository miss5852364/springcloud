server:
  port: 7000
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: provider-nacos
management:
  endpoints:
    web:
      exposure:
        include: "*"